function solve (word) {

    for (let index=0; index<word.length; index++) { // (word.length) е с 1 по-голямо от броя символи (index)
        console.log (word[index]) // изпиши думата, разделяйки я на позиции (индекси)
    }
}

solve ("hello")
