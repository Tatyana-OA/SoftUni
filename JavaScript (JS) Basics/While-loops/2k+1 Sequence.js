function solve (input) {
    let firstnum=Number(input[0]); 
    let start = 1; 
    while (start<=firstnum) { 
        console.log(start)
    start=start*2+1; 
    

    }
}

// for (let start=1; start <= firstnum; start = 2*start + 1) 




solve (["3"])
