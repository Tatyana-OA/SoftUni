function solve () {
    // Boolean values - > true/false

    let firstNum=3;
    let secondNum = 3; // '3' - string, so === will give 'False" but == will give TRUE
    let isBigger = firstNum > secondNum;
    
    if (isBigger) {
        console.log("First number is smaller than or equal to the second number")
    }
    
    
}
solve ();
