function greaterNumber (num1, num2) {
    numberFirst = Number(num1);
    numberSecond = Number(num2);

    if (numberFirst>numberSecond) {
        console.log (numberFirst)
    } else {
        console.log (numberSecond)
    }
}
greaterNumber("-5", "5")
