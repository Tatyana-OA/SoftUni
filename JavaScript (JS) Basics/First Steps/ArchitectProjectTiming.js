function projects (architect, projectCount){
    let hoursTotal = 3*projectCount;

    console.log(`The architect ${architect} will need ${hoursTotal} hours to complete ${projectCount} project/s.`)
}

projects("George","4")
