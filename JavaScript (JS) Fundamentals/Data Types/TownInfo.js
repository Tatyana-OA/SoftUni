function solve (town, one, two) {
    let population =  Number(one);
    let area = Number(two);
    console.log (`Town ${town} has population of ${population} and area ${area} square km.`)
    
}

solve('Plovdiv',20000,250)
