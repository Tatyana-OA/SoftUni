function solve(input) {
    let string = input.toString(); // от число към стринг
    let sum = 0

    for (let i= 0; i<string.length; i++) { // обхождане на стринг
        let character = Number(string[i]) // всяко число от поредицата (въртим до дължината)
   
        sum += character;
       

  
}  console.log (sum)
} solve (543)
