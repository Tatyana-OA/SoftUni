function personInfo(firstName, lastName, age){
let person = {} // empty object
person.firstName=firstName;
person.lastName=lastName;
person.age=age;
// fill in the empty object with key + value from the input

return person;

}
